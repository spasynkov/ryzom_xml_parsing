package utils;

import java.text.SimpleDateFormat;

/**
 * Simple class for date formatting
 */
public class DateFormatter {
    private static String dateFormattingPattern = "";
    private static SimpleDateFormat format = new SimpleDateFormat(dateFormattingPattern);

    public static String getDateAndTimeInUtc(long timestamp) {
        String result = "";
        return result;
    }
}
